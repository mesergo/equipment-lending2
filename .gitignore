node_modules/
build/
dist/
server-build/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local auth server data (seeded user accounts with hashed passwords) — never commit real credentials
server/data/

# Contains a live SFTP host/username/password for the production deploy target — never commit
.vscode/sftp.json
